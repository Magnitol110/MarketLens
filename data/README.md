# Data policy

`raw/` contains untouched, date-stamped snapshots downloaded from approved sources. `processed/` contains generated tables and features. Neither folder is committed to Git.

Before using any source, add it to `../docs/sources.csv` with its URL, licence, retrieval date, fields, and decision.

The final presentation must state the exact snapshot date used by the application.
